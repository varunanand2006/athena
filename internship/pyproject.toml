[project]
name = "internship-hunter"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "apscheduler>=3.10,<4",
    "httpx>=0.27",
    "psycopg2-binary>=2.9",
    "prometheus-client>=0.20",
]
