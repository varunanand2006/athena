server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    client_max_body_size 20m;

    # SSE streaming endpoint — must come before the general /chat block
    location = /chat/stream {
        proxy_pass http://agent.athena.svc.cluster.local:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_http_version 1.1;
        proxy_set_header Connection '';
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 300s;
        chunked_transfer_encoding on;
    }

    # Proxy agent endpoints
    location ~ ^/(chat|conversations|internships|leetcode|healthz|documents|system|memory) {
        proxy_pass http://agent.athena.svc.cluster.local:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 120s;
    }

    # Proxy ingestion endpoints (uploads + TOC)
    location ~ ^/(ingest|toc) {
        proxy_pass http://ingestion.athena.svc.cluster.local:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_read_timeout 180s;
        proxy_request_buffering off;
    }

    # SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }
}
