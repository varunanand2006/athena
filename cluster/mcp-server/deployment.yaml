apiVersion: apps/v1
kind: Deployment
metadata:
  name: mcp-server
  namespace: athena
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mcp-server
  template:
    metadata:
      labels:
        app: mcp-server
    spec:
      # Co-locate with the agent it proxies. Every MCP tool call becomes
      # an in-cluster HTTP call to the agent; keeping them on the same
      # node avoids cross-node hops on the hot path. The agent itself is
      # pinned to xdev-sr via workload=ai, so we match that label.
      nodeSelector:
        workload: ai
      enableServiceLinks: false
      containers:
        - name: mcp-server
          image: athena-mcp-server:phase13
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: AGENT_BASE_URL
              value: "http://agent.athena.svc.cluster.local"
            - name: BIND_ADDRESS
              value: "0.0.0.0:8080"
            # rmcp's DNS-rebinding guard rejects any Host: not on this list.
            # `mcp.athenamcp.uk` is the Phase 13 Cloudflare Tunnel public
            # hostname — Cloudflare forwards the original Host header to the
            # origin, so the tunneled requests arrive with that value.
            - name: ALLOWED_HOSTS
              value: "mcp.local,mcp.local:80,localhost,127.0.0.1,0.0.0.0,mcp.athenamcp.uk"
            # Phase 13: bearer token for the auth middleware. Sourced from a
            # k8s Secret (not committed). Unset → middleware fails closed and
            # rejects every /mcp request.
            - name: MCP_AUTH_TOKEN
              valueFrom:
                secretKeyRef:
                  name: mcp-auth
                  key: token
            - name: RUST_LOG
              value: "info,athena_mcp_server=debug"
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 6
