apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ingestion-documents
  namespace: athena
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 10Gi
